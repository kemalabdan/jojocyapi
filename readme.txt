DESCRIPTION
This automation is build using the library from cypress.io.

HOW TO RUN
How to run it is using the 'yarn init' in the directory of this folder, end then 'yarn run cyopen' or 'yarn run cyrun' in the terminal, so it will start running
In the folder integration, there is a folder named "api" for answering the test case in the instruction of the technical test
